<%@ page language="java" contentType="text/html; charset=UTF-8"
    pageEncoding="UTF-8"%>
<%@ taglib uri="http://java.sun.com/jsp/jstl/core" prefix="c"%>
<%
	String path = request.getContextPath();
%>
<!DOCTYPE html PUBLIC "-//W3C//DTD HTML 4.01 Transitional//EN" "http://www.w3.org/TR/html4/loose.dtd">
<html>
    
    <head>
        <meta charset="utf-8">
        <title>
            金字塔 - 综合金融服务后台管理 - 角色列表 - 修改
        </title>
        <base href="<%=path%>/">
        <meta name="renderer" content="webkit">
		<meta http-equiv="Content-Type" content="text/html; charset=UTF-8">
        <meta http-equiv="X-UA-Compatible" content="IE=edge,chrome=1">
        <meta name="viewport" content="width=device-width, initial-scale=1, maximum-scale=1">
        <meta name="apple-mobile-web-app-status-bar-style" content="black">
        <meta name="apple-mobile-web-app-capable" content="yes">
        <meta name="format-detection" content="telephone=no">
        <script type="text/javascript" src="js/jquery-1.8.3.min.js"></script>
        <link rel="stylesheet" href="./css/x-admin.css" media="all">
    	<style type="text/css">
			a:focus{text-decoration: none}
			a:focus{outline:none;}
		</style>
    </head>
    
    <body>
    	<input type="hidden" value="${roleId }" id="roleId" />
        <div class="x-body">
            <form  method="post" class="layui-form layui-form-pane">
                <div class="layui-form-item layui-form-text">
                    <label class="layui-form-label">
                        拥有权限
                    </label>
                    <table  class="layui-table layui-input-block">
                        <tbody>
                            <tr>   
	                            <c:forEach items="${jurExt }" var="jur">
			                        <c:if test="${jur.id ==1}">
				                        		  <td width="5%">${jur.name }</td>
			                      	</c:if>     
	                            </c:forEach>
                             <td>   
			                     <div class="layui-input-block">
		                              <c:forEach items="${jurExt }" var="jur">
		                                   <c:if test="${jur.pid ==1 }">
			                                  <input name="jurId"  type="checkbox" value="${jur.id }" <c:if test="${jur.roleJur.checked==true }">checked</c:if>> ${jur.name }
		                                  </c:if>
		                              </c:forEach>
			                    </div>
			                 </td>
                            </tr>
                            <tr>   
	                            <c:forEach items="${jurExt }" var="jur">
			                        <c:if test="${jur.id ==2}">
				                        		  <td>${jur.name }</td>
			                      	</c:if>     
	                            </c:forEach>
                             <td>   
			                     <div class="layui-input-block">
		                              <c:forEach items="${jurExt }" var="jur">
		                                   <c:if test="${jur.pid ==2 }">
			                                  <input name="jurId"  type="checkbox" value="${jur.id }" <c:if test="${jur.roleJur.checked==true }">checked</c:if>> ${jur.name }
		                                  </c:if>
		                              </c:forEach>
			                    </div>
			                 </td>
                            </tr>
                            <tr>   
	                            <c:forEach items="${jurExt }" var="jur">
			                        <c:if test="${jur.id ==3}">
				                        		  <td>${jur.name }</td>
			                      	</c:if>     
	                            </c:forEach>
                             <td>   
			                     <div class="layui-input-block">
		                              <c:forEach items="${jurExt }" var="jur">
		                                   <c:if test="${jur.pid ==3 }">
			                                  <input name="jurId"  type="checkbox" value="${jur.id }" <c:if test="${jur.roleJur.checked==true }">checked</c:if>> ${jur.name }
		                                  </c:if>
		                              </c:forEach>
			                    </div>
			                 </td>
                            </tr>
                            <tr>   
	                            <c:forEach items="${jurExt }" var="jur">
			                        <c:if test="${jur.id ==4}">
				                        		  <td>${jur.name }</td>
			                      	</c:if>     
	                            </c:forEach>
                             <td>   
			                     <div class="layui-input-block">
		                              <c:forEach items="${jurExt }" var="jur">
		                                   <c:if test="${jur.pid ==4 }">
			                                  <input name="jurId"  type="checkbox" value="${jur.id }" 
			                                  <c:if test="${jur.roleJur.checked==true }">checked</c:if>> ${jur.name }
		                                  </c:if>
		                              </c:forEach>
			                    </div>
			                 </td>
                            </tr>
                            
                            <tr>   
	                            <c:forEach items="${jurExt }" var="jur">
			                        <c:if test="${jur.id ==5}">
				                        		  <td>${jur.name }</td>
			                      	</c:if>     
	                            </c:forEach>
                             <td>   
			                     <div class="layui-input-block">
		                              <c:forEach items="${jurExt }" var="jur">
		                                   <c:if test="${jur.pid ==5 }">
			                                  <input name="jurId"  type="checkbox" value="${jur.id }" 
			                                  <c:if test="${jur.roleJur.checked==true }">checked</c:if>> ${jur.name }
		                                  </c:if>
		                              </c:forEach>
			                    </div>
			                 </td>
                            </tr>
                            <tr>   
	                            <c:forEach items="${jurExt }" var="jur">
			                        <c:if test="${jur.id ==6}">
				                        		  <td>${jur.name }</td>
			                      	</c:if>     
	                            </c:forEach>
                             <td>   
			                     <div class="layui-input-block">
		                              <c:forEach items="${jurExt }" var="jur">
		                                   <c:if test="${jur.pid ==6 }">
			                                  <input name="jurId"  type="checkbox" value="${jur.id }" 
			                                  <c:if test="${jur.roleJur.checked==true }">checked</c:if>> ${jur.name }
		                                  </c:if>
		                              </c:forEach>
			                    </div>
			                 </td>
                            </tr>
                            <tr>   
	                            <c:forEach items="${jurExt }" var="jur">
			                        <c:if test="${jur.id ==7}">
				                        		  <td>${jur.name }</td>
			                      	</c:if>     
	                            </c:forEach>
                             <td>   
			                     <div class="layui-input-block">
		                              <c:forEach items="${jurExt }" var="jur">
		                                   <c:if test="${jur.pid ==7 }">
			                                  <input name="jurId"  type="checkbox" value="${jur.id }" 
			                                  <c:if test="${jur.roleJur.checked==true }">checked</c:if>> ${jur.name }
		                                  </c:if>
		                              </c:forEach>
			                    </div>
			                 </td>
                            </tr>
                        </tbody>
                    </table>
                </div>                
                <div class="layui-form-item">
                <button class="layui-btn" lay-submit="" lay-filter="save">保存</button>
              </div>
            </form>
        </div>
        <script src="./lib/layui/layui.js" charset="utf-8">
        </script>
        <script src="./js/x-layui.js" charset="utf-8">
        </script>
        <script>
            layui.use(['form','layer'], function(){
                $ = layui.jquery;
              var form = layui.form()
              ,layer = layui.layer;

              //监听提交
              form.on('submit(save)', function(data){
                console.log(data);
                //发异步;，把数据提交给后台
                //获取角色id
               var roleId = $("#roleId").val();
               var tollerlist = new Array();//建立数组
		       	 //取得选中复选框值
		       	 $('input[name="jurId"]:checked').each(function(){
		                var sfruit=$(this).val();//取值
		                tollerlist.push(sfruit)//赋值
		                });
               
		        if(tollerlist.length==0)
	              { tollerlist.push(0)}
               
		       	$.ajax({
	    			 async:false,
	    		 	 cache:true,
	    		 	 url:'manager/updateJur',
	    		 	 type:'post',
	     		 	data:{
	     		 		"roleId":roleId,
	     		 		"tollerlist":tollerlist
	     		 	},
	    		 	 success:function(result){
	    		 		 // 获得frame索引
	                     var index = parent.layer.getFrameIndex(window.name);
	                     //关闭当前frame
	                     parent.layer.close(index);
	    		 	 },
	    		 	 error:function(XMLHttpRequest,textStatus){
	    		 		 alert("错误");
	    		 	 }
	    		 });
              });
            });
            
            
        </script>
        
    </body>

</html>